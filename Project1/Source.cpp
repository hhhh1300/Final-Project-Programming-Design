#include <ctime>
#include "Game.h"

/*
int main()
{
    Game game;
 
    game.playerPosition();
    game.player2Position();
    game.ballPosition();

    std::clock_t initSecond = clock();

    while (game.running())
    {
        std::clock_t second = 360 - (clock() - initSecond) / CLOCKS_PER_SEC;
        if (!game.timer(second))
        {
            return 0;
        }
        game.update();
        game.render();
    }
    
    
    return EXIT_SUCCESS;
}
*/